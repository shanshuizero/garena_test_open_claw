# 开发英语词汇总目录

> 用于统一记录已收集的专业词汇，方便查找、复习和持续补充。

## 1. 通用开发核心词汇

architecture, module, component, dependency, environment, configuration, integrate, implement, maintain, refactor, reusable, scalable, robust, efficient, compatible, version, repository, branch, merge, deploy, release, issue, bug, feature, requirement, solution, validate, monitor, optimize

## 2. 前端开发词汇

layout, responsive, breakpoint, viewport, render, rendering, re-render, hydration, bundle, asset, stylesheet, selector, specificity, animation, transition, accessibility, semantic, state, props, hook, event, routing, middleware, debounce, throttle, lazy loading, tree shaking

## 3. 前端专项词汇（React / Vue / TypeScript / 浏览器 / 性能）

virtual DOM, reconciliation, key, controlled component, uncontrolled component, context, prop drilling, memoization, side effect, lifecycle, composable, reactive, computed property, watcher, type inference, type annotation, generic, union type, narrowing, immutable, event loop, call stack, microtask, repaint, reflow, critical rendering path, code splitting, prefetch, preload, long task

## 4. 前端深入词汇（React）

hook dependency, stale closure, useMemo, useCallback, React.memo, lifting state up, server-side rendering, static generation, hydration mismatch, suspense, concurrent rendering, custom renderer, controlled input lag, state normalization, render prop

## 5. 前端深入词汇（Vue）

Composition API, Options API, ref, reactive object, computed caching, watchEffect, slot, scoped slot, directive, Pinia store, route guard, keep-alive, transition group, template ref, single-file component

## 6. 前端深入词汇（TypeScript）

interface, type alias, extends, intersection type, mapped type, conditional type, infer, utility type, type guard, discriminated union, declaration file, ambient declaration, strict mode, non-null assertion, generic constraint

## 7. 前端深入词汇（浏览器 / 网络 / 性能）

DNS lookup, TCP handshake, TLS negotiation, cache-control, cache hit ratio, content delivery network, resource hint, preconnect, time to first byte, first contentful paint, largest contentful paint, cumulative layout shift, interaction to next paint, render-blocking resource, main thread bottleneck

## 8. 后端开发词汇

endpoint, service, controller, middleware, authentication, authorization, token, session, cache, queue, worker, retry, timeout, concurrency, transaction, persistence, serialization, deserialization, throughput, latency, failover, recovery, idempotent, cron job, rate limit, circuit breaker

## 9. 后端专项词汇（微服务 / 分布式 / 数据库 / 消息系统）

microservice, service boundary, service contract, downstream, upstream, service mesh, API gateway, fan-out, cascading failure, backpressure, dead letter queue, consumer group, partition key, offset, exactly-once, at-least-once, at-most-once, read replica, primary key, foreign key, denormalization, query plan, connection pool, thread-safe, race condition, lock contention, eventual retry, hot partition, write amplification, quorum

## 10. 后端深入词汇（Java）

dependency injection, bean lifecycle, annotation-driven, transaction propagation, isolation level, lazy initialization, eager loading, N+1 query problem, thread pool, garbage collection, heap dump, classpath, servlet filter, interceptor, autowiring

## 11. 后端深入词汇（Go）

goroutine, channel, buffered channel, select statement, context cancellation, deadline exceeded, interface satisfaction, zero value, receiver, race detector, panic recovery, worker pool, memory allocation, escape analysis, graceful shutdown

## 12. 后端深入词汇（Node.js）

event-driven architecture, non-blocking I/O, callback hell, promise chain, async pipeline, stream backpressure, middleware chain, memory leak, heap snapshot, event emitter, listener leak, worker thread, process manager, cluster mode, unhandled rejection

## 13. 后端深入词汇（数据库 / 事务）

dirty read, non-repeatable read, phantom read, snapshot isolation, row-level lock, table lock, pessimistic locking, optimistic locking, index scan, full table scan, composite index, covering index, write skew, deadlock, rollback segment

## 14. 后端深入词汇（系统设计面试）

write path, read path, bottleneck analysis, capacity planning, workload pattern, hot key, request amplification, fault domain, blast radius, graceful degradation, read-after-write consistency, eventual convergence, write-heavy workload, read-heavy workload, cost efficiency

## 15. API 与数据库词汇

request, response, payload, schema, validation, parameter, query, mutation, pagination, filtering, sorting, relational database, index, migration, replica, shard, backup, restore, consistency, availability, partition tolerance

## 16. DevOps 与部署词汇

container, image, orchestration, pipeline, artifact, staging, production, rollback, observability, logging, metrics, tracing, alert, incident, infrastructure, provisioning, secret, certificate, reverse proxy, load balancer

## 17. 调试、评审与协作词汇

debug, reproduce, isolate, diagnose, root cause, workaround, patch, review, comment, approval, suggestion, concern, blocking, non-blocking, clarify, align, estimate, milestone, ownership, handoff, edge case, trade-off, backward compatible

## 18. 测试与质量词汇

test case, unit test, integration test, end-to-end test, assertion, coverage, mock, stub, regression, flaky

## 19. 安全工程词汇

vulnerability, encryption, decryption, secret, permission, audit log, sanitize, escape, authentication bypass, least privilege, penetration test, exploit, incident, mitigation, remediation

## 20. 系统设计与云原生词汇

availability, consistency, partition tolerance, replica set, leader election, distributed lock, eventual consistency, horizontal scaling, vertical scaling, stateless, stateful, service discovery, load balancer, reverse proxy, orchestration, node, pod, autoscaling, health check, disaster recovery, single point of failure, multi-region, cold start, resilience, fallback

## 21. 高频动词短语

set up, break down, roll out, scale up, scale out, fall back, clean up, spin up, tear down, plug in, log in, log out, back up, point to, depend on, map to

## 22. 高频会议表达

Let me clarify.
From the frontend perspective...
From the backend side...
This issue is caused by...
We need to verify whether...
The root cause is still under investigation.
Can we reproduce this issue consistently?
We should add monitoring for this case.
This change is backward compatible.
We may need to refactor this module.
I am mainly responsible for...
We solved this by...
There is a risk that...
A possible improvement would be...
Please correct me if I am wrong.
I will follow up on this.

## 23. 真实工作场景对话主题

daily stand-up, requirement review, API integration, PR review, incident response, database performance discussion, system design discussion, project introduction, clarifying requirements, disagreeing professionally

## 24. 进阶工作场景对话主题

architecture review, cross-team alignment, postmortem discussion, QA collaboration, timeline negotiation, technical disagreement, speaking up in a global meeting

## 25. 书面表达主题

daily update, weekly update, pull request description, pull request comment, issue response, meeting notes, follow-up message

## 26. 面试主题

tell me about yourself, biggest strength, technical challenge, team disagreement, why this company, scalable system design, production debugging, good code, task prioritization, questions for interviewers
